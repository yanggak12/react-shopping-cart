{
  "name": "react-shopping-cart",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "yarn workspace client",
    "server": "yarn workspace server"
  },
  "workspaces": [
    "packages/*"
  ]
}
